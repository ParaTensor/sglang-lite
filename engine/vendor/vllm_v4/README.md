# vendor/vllm_v4 — vLLM V4 / MLA / MoE slices

**Status**: placeholder.

Copy only if a slice is faster/clearer than SGLang for SM120 V4-Flash.
No full engine, multimodal, or scheduler.

Import rewrite: `vllm...` → `sglang_lite.vendor.vllm_v4...`.

Pin every refresh in `docs/vendor/SOURCES.md`.
